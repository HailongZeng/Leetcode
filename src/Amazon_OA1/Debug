1. sortArray        把if里面的>换成<
2. countOccurence   while循环i++
3. checkGrade gradingsystem    （x>=70)||(x<80)，两个||改成&&
4. reverse Array    改成len-=1
5. replaceValue     改成i<len, j<len
6. printPattern     2个for加括号  // ch++改为print++
7. printCharacter   ch++改成print++
8. distinctNum      ==改为!=
9. sumDistinct      初始化sum写在sort之后
10. eliminateVowel  default去掉i++
11. checkPalindrome 把最后返回值改成return (result==temp);
12. distinctElementCount  if(flag == 1)改成if(flag == 0)
13. matrixSum       while(j<n) sum += matrix[i++][j++];改成while(j<n) {sum += matrix[i][j++];} i++;
14. labelProduct    加入字符串的方向反了
15. medianValue     arr2越界，改为arr2[i-size]   else arr[i] = arr2[i]改为else arr[i] = arr2[i-size]
16. countDays       改为((y % 4 == 0 && (y % 100) != 0)
17. countElementRange  把||改成&&
18. countNumParity getDigitSumParity DigitSum  (1)主函数result % 2 == 0  (2)主函数if(result==0) {return 1} (3)sumDigit函数把int temp=num%10和num=num/10换一下位置
19. countProduct     for (j = 0; j < valueofK; j++)改为for(j = 0; j < size; j++)
20. countElement     改成i<len; i++  后面arr[i+1]改成arr[i]
21. checkArmstrong   result+=math.pow(result,digitCount)应该改为result+=math.pow(remainder,digitCount)
22. reverseNumber    应该是reverseNum=reverseNum*10+Reminder
23. appearsKTimes    while loop结束后添加  if(count==k){res=element};
24. reverseString    去掉len++，不是str+=
25. sortArray descendingSortArray  if (max > arr[j])改成<
26. selectionSort/selectionSortArray    ascending order, arr[index_of_min]>arr[x]改为arr[index_of_min] > arr[y]
27. drawPrintPattern  第二个for loop后面的分号删掉
28. vowelsString     while循环i++放在外面   while循环少了i++ if(vcount>len%2)改为if(vcount>(len/2))
29. mergeTwoLists    else if(l1.val<=l2.val){l1.next=mergeTwoLists(l1.next, l2); return l1;}
30. arraySum        sum = arr[i]改为sum += arr[i]
31. Remove element  for(i=index;i<len-1;i++){arr[i]=arr[i++]}改为arr[i]=arr[i+1]
32. Even odd pattern    even和odd处的for loop缺少大括号
33. Manchester  这个有两处错误，是要判断 array 里每位数字和前面一位是不是相等，index0 的数字单独和 0 判断一下，所以 0 那句不能直接 res[0] = 0 要 res[0] = (arr[0] != 0) ? 1:0，后面 for
  循环里判断是要把 arr == arr[i-1]改成 arr != arr[i-1]
34. Remove duplicate from unsorted array   报错是 Index out of bound, 因为 for 循环里有 k<length, 下面却使用 arr[k+1] 改为 k < length - 1
35. Remove duplicates   循环下标从i+1开始
36. Insertion sort   if (arr[i-1] > arr[i])   while(j>0&&arr[j-1]>temp)
37. countA   if(c=="A"||c=="a")
38. countDigits   这里面到最后 num 直接是 0，造成 bug，需要用一个数来储存 num 和执行运算
39. arrayOperation    <和>写反了


ArrayOperation  将i<=len和j<=len改为i<len和j<len
sortArray
Occurrence
printPattern
GradingSystem
DrawPattern
selectionSort
